const buttons = document.querySelectorAll(".number");
const totalDisplay = document.getElementById("calc");
let total = 0;

buttons.forEach(button => {
    button.addEventListener("click", (e) => {
        total = join[]
        totalDisplay.textContent = total; // Atualiza o total na tela
    });
});

